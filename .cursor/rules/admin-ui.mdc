---
description: jsonConfig admin UI and i18n for ioBroker.life360ng
globs: admin/**
alwaysApply: false
---

# Admin UI (jsonConfig)

## Files

- `admin/jsonConfig.json` — UI definition (source of truth for labels/help)
- `admin/i18n/de.json`, `admin/i18n/en.json` — **manually maintained only**
- All other languages (`es`, `fr`, `it`, `nl`, `pl`, `pt`, `ru`, `uk`, `zh-cn`) — generated by owner via `npm run translate`; do **not** edit manually

## jsonConfig guidelines

- End-user friendly labels; tooltips for complex options (e.g. Bearer token setup for EU users)
- Sensible defaults; validation on required fields
- Images in `admin/img/`, referenced as `img/<filename>` in `staticImage` elements
- Every jsonConfig field must exist in `io-package.json` → `native` with a default

## Translations workflow

When changing `admin/jsonConfig.json`:

1. Add new keys to `admin/i18n/de.json` (German) and `admin/i18n/en.json` (English)
2. Remove orphaned keys from `de.json` and `en.json` when keys are removed from jsonConfig
3. Keys must match jsonConfig exactly; sort alphabetically
4. Do **not** run `npm run translate` — owner runs it before release

## Key config fields

- `life360_token` (text): Bearer token from browser DevTools (no `Bearer` prefix in config)
- `life360_polling_interval` (number): poll interval in seconds (min 15, max 1440, default 60)
- `places` (table, `_myplaces` tab): custom private places (name, lat/lon, radius)
- `process_life360_circles` / `process_life360_places` / `process_life360_people`: integration toggles
- `track_location_people`: geo details on people data points
- `tracker_*`: location tracking and HTML map display options
- Notification settings: Telegram/Alexa and per-datapoint notification overrides
